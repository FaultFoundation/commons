# dependencies / build
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# wrangler local state
.wrangler/

# OpenNext build output
.open-next/

# local dev secrets (see .dev.vars.example)
.dev.vars

# misc
.DS_Store
.vscode/
logs/
temp/
.claude/
.turbo/
*.log

# NOTE: drizzle/ is NOT ignored on purpose — the generated *.sql migrations and
# meta/ snapshots are the schema history that `wrangler d1 migrations apply`
# runs, so they must be committed. Only the local D1 *state* (.wrangler/) and
# build output are temporary.

# local-only reference material (not part of the commons app)
reference/
wordpress site/

# generated by scripts/cen-project.mjs — a local import artifact, never committed
cen-seed.sql
